---
/**
 * Disabled generic WP Pages route.
 * Only the About page is supported via [astro.page(about.astro)](src/pages/about.astro:1).
 * This file intentionally generates no pages.
 */
export async function getStaticPaths() {
  return [];
}
---